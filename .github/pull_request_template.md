# What & why

# Checklist

- [ ] `pnpm check` passes locally
- [ ] New code is covered by tests (coverage thresholds are enforced)
- [ ] Docs updated if behavior or configuration changed (README, docs/, CLAUDE.md)
